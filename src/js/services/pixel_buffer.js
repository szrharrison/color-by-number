import RGBA from "./rgba";

const fill = function () {
  this.filled = true;
  return this;
};

function PixelBuffer(id, color, imageX, imageY, canvasX = 0, canvasY = 0, filled = 0) {
  if (color.)
  return new Uint16Array(id, color.number, imageX, imageY, canvasX, canvasY, filled);
}

export default PixelBuffer;
